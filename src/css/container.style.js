import {StyleSheet} from 'react-native';

export const Styles = StyleSheet.create({
    container: {
        flex:1,
        padding:10
    },
    viewContainer : {
        flex:1,
        padding:5,
        backgroundColor:'white',
        borderRadius:10
    }
})